{
  "name": "rrweb-recorder-extension",
  "version": "1.0.0",
  "description": "Chrome extension to record web pages using rrweb",
  "scripts": {
    "build": "bun build ./background.ts --outdir=dist && bun build ./inject.ts --outdir=dist && cp manifest.json dist/"
  },
  "dependencies": {
    "rrweb": "^2.0.0-alpha.4"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.3"
  }
}
